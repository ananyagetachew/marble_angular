import { AddDToDeliveryNoPipe } from './add--d--to-delivery-no.pipe';

describe('AddDToDeliveryNoPipe', () => {
  it('create an instance', () => {
    const pipe = new AddDToDeliveryNoPipe();
    expect(pipe).toBeTruthy();
  });
});
