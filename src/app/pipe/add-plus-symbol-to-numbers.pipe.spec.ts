import { AddPlusSymbolToNumbersPipe } from './add-plus-symbol-to-numbers.pipe';

describe('AddPlusSymbolToNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new AddPlusSymbolToNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
