import { AddPToProformaNoPipe } from './add--p--to-proforma-no.pipe';

describe('AddPToProformaNoPipe', () => {
  it('create an instance', () => {
    const pipe = new AddPToProformaNoPipe();
    expect(pipe).toBeTruthy();
  });
});
